spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file://${HOME}/Documents/MyProjects/Cricketers-Stats
          search-paths: configurations
server:
  port: 8888
